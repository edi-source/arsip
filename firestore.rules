rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow anyone to read and write to facilitate real-time demo in sandboxed environments
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
